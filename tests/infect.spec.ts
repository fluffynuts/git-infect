import "expect-more-jest";
// import { Sandbox } from "filesystem-sandbox";

describe(`infect`, () => {
    it(`should infect a single victim from patient zero`, async () => {
        // Arrange
        // const
        //     sandbox = new Sandbox()

        // Act
        // Assert
    });
});
